[package]
name = "deflate_coder"
version = "0.1.0"
edition = "2024"

[lib]
lib = "src/lib.rs"

[dependencies]
bincode = "1"
serde = { version = "1", features = ["derive"] }
libflate = "2"
anyhow = "1.0.100"
